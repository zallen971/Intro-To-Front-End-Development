// create variable named score and set it to 8
var score = 8;

// use console.log() that includes the string "Mid-level skills:" and compares the score variable to abov 0 and below 10 using the && operator
console.log("Mid-level skills: " + (score > 0 && score < 10));


// declare the variable timeRemaining and assign the value 0
var timeRemaining = 0;

// declare the variable energy and assign the value 10
var energy = 10;

// console log the following parameters: "Game over: " and timeRemaining === 0 || energy == 0
console.log("Game over: " + (timeRemaining === 0 || energy == 0));




// variable num1 assigned 2
var num1 = 2;

// variable num2 assigned 5
var num2 = 5;

// third variable test1 should be assigned the calculation num1 % 2
var test1 = num1 % 2;

// test2 should be assigned the calculation num2 % 2
var test2 = num2 % 2;

// result1 should be assigned the result of comparing if the number stored in test1 variable is not equal to 0
var result1 = test1 == 0;

// result2 should be assigned the result of comparing if the number stored in test2 variable is not equal to 0
var result2 = test2 == 0;

console.log("Is", num1, "an even number?", result1);
console.log("Is", num2, "an even number?", result2);




// console log the result of adding two numbers, 5 and 10 using the + operator
console.log(5 + 10);




// the first variable should be a string with the following value: "Now in "
var now = "Now in ";

// the second variable should be the number 3
var three = 3;

// the third variable should be a string with the following value: "D!"
var d = "D!";

// console log the following code: now + three + d
console.log(now + three + d);



var counter = 0; // new variable named counter with 0 as assigned value

counter += 5; // increment value by 5
counter += 3; //increment value by 3

console.log(counter); // console log the counter