// declare a new variable named petDog and give it the name Rex
var petDog = "Rex";

// declare a new variable named petCat and give it the name Pepper
var petCat = "Pepper";

// log tje petDog variable to the console
console.log(petDog);

// log the petCat variable to the console
console.log(petCat);

// log the following to the console: the text "My pet dog's name is: " and the value of the petDog variable
console.log("My pet dog's name is: ", petDog);

// log the following to the console: the text "My pet cat's name is: " and the value of the petCat variable
console.log("My pet cat's name is: ", petCat);

// declare another variable named catSound. Assign the string "purr"
var catSound = "purr";

// declare another variable named dogSound. Assign the string "woof"
var dogSound = "woof";

// log the following to the console: the variable petDog, then the string "says" and then the variable dogSound
console.log(petDog, "says", dogSound);

// log the following to the console: the variable petCat, then the string "says" and then the variable catSound
console.log(petCat, "says", catSound);

// reassign the value stored in catSound to the string "meow"
catSound = "meow";

// log the following to the console: the variable petCat, then the string "now says" and then the updated variable catSound
console.log(petCat, "now says", catSound);